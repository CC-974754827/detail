```
1、图标制作 ico
favicon.ico
<link rel="shortcut icon" href="">

2、导航、列表用ul，ol
3、后定义的会把先定义的覆盖掉

4、#000000   红绿蓝

5、100%  %相对于父元素

6、float:浮动元素脱离文档流
元素塌陷:父元素高度为0，浮动元素脱离文档流
(子元素掉落，说明父元素宽度不够)
解决方法：①为父元素加高度
②clear:left; 清除浮动
③定位


7、line-height = height 单行文字垂直居中
```
